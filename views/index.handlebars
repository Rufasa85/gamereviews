<h1>Homepage</h1>

<h2>All reviews:</h2>
{{#each reviews}}
    <h3>title: {{title}}, for {{players}} players</h3>
    <h4>by {{User.name}}</h4>
    <p>review: {{review }}</p>
    <h4>score:  {{rating}}/10</h4>
    <hr>
{{/each}}